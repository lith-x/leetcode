{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "command": "gcc",
            "options": {"cwd": "${workspaceFolder}/prob023"},
            "type": "shell",
            "args": ["main.c", "-g", "-o", "main"],
            "group": "build"
        }
    ]
}